// Unused. Search and category filtering live in the Week 12 chrome instead:
// the header search bar (keyword) and the "Explore top categories" cards
// (category). Online vs in-person is the two-section split on the home page.
// Kept as an empty module so no stray import breaks the build.
export default function EventFilters() {
  return null;
}
